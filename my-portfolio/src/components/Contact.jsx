import { projects } from '../data/project';
import ProjectCard from './ProjectCard';

function Contact() {
    return (
        <section id="contact" className="border-t border-line px-4 py-4 sm:px-8 sm:py-8">
            <h1>
                This contact section
            </h1>
        </section>
    );
}

export default Contact;